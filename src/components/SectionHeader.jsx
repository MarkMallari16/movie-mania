import React from 'react'

const SectionHeader = ({ title }) => {
    return (
        <h2 className='text-lg text-secondary font-medium mt-6 mb-4'>
            {title}
        </h2>
    )
}

export default SectionHeader